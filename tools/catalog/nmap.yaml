id: nmap
name: nmap
description: >
  Network scanner for authorized in-scope host discovery and port enumeration.
  Linked skill: network-scanner. Install via apt below.
tier: catalog
binary: nmap
install:
  - type: apt
    packages: [nmap]
verify:
  - command: "nmap --version"
skills: [network-scanner]
tags: [recon]
