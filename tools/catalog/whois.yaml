id: whois
name: whois
description: >
  WHOIS lookups for domains and IP attribution. Linked skill: domain-enum.
  Install via apt below.
tier: catalog
binary: whois
install:
  - type: apt
    packages: [whois]
verify:
  - command: "whois --version 2>/dev/null || which whois"
skills: [domain-enum]
tags: [recon, osint]
