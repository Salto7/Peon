id: dnsutils
name: dnsutils
description: >
  dig and nslookup for DNS queries. Linked skill: domain-enum. Install via apt below.
tier: catalog
binary: dig
binaries: [dig, nslookup]
install:
  - type: apt
    packages: [dnsutils]
verify:
  - command: "dig -v 2>/dev/null || which dig"
skills: [domain-enum]
tags: [recon]
