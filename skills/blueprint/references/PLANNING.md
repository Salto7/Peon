# blueprint planning notes

## Deliverables (extra)
- Project: also under `workspace/plans/` when applicable

## Project-aware content
- Prefer skills whose tags/categories match the objective (`skills_list` / `skill_view`)
- Optional role (`profile_suggestion`) for the job that will run an objective
- `workspace/` = intermediate; `findings/` = curated; final report = `findings/report.md`
- Order objectives with skill prerequisites + `depends_on`
- ≥2 independent phases → plan one subagent job each (no duplicates unless asked)
- If hybrid planning already wrote `plans/latest.md`, refine in place when asked
